name: 'student'
version: 1.0.0
config-version: 2
profile: 'default'

models:
  student:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    mart:
      +materialized: table
      finance:
        +schema: finance